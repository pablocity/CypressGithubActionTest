name: Build Checks
on: push

jobs:
  cypress-tests:
    runs-on: ubuntu-latest
    - name: Checkout
      uses: actions/checkout@v2
    - name: Cypress tests run
      uses: ./.github/actions/action.yml
