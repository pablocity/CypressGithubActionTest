name: Build Checks
on: push

jobs:
  cypress-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cypress tests run
        uses: pablocity/CypressGithubActionTest/.github/actions/action.yml
        with:
          default-branch: main
